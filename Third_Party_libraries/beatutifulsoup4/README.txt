Libraries which are going to use for this chapters

1) pip install beautifulsoup4
2) pip install lxml  (To parse the html which is having mistakes)
3) pip install html5lib (Another popular library to parse the html)
4) pip install requests (To deal with urls)
